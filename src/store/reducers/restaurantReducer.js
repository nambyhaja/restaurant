const initState = {
    restaurants: []
};

const restaurantReducer = (state = initState, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default restaurantReducer;